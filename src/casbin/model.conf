[request_definition]
r = sub, obj, act, org

[policy_definition]
p = sub, obj, act, org

[role_definition]
g = _, _

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = g(r.sub, p.sub) && r.obj == p.obj && r.act == p.act && (p.org == "*" || p.org == "org" || p.org == r.org)
